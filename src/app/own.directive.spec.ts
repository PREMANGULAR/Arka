import { OwnDirective } from './own.directive';

describe('OwnDirective', () => {
  it('should create an instance', () => {
    const directive = new OwnDirective();
    expect(directive).toBeTruthy();
  });
});
